# Stripe 本番切替 手順書（銀行振込・先着10店舗価格）

作成 2026-08-06。**本物のお金が動く作業**なので、順番を守ってください。

---

## ✅ 実施状況（2026-08-06 17時時点）

| 手順 | 状態 |
|---|---|
| 1. 本番価格の作成 | ✅ 完了 `price_1U1LiiEsR4aDysj985YR1IOf`（8,140円） |
| 2. Webhookの本番エンドポイント | ✅ 完了。**既存の送信先が古い開発プロジェクトを向いていたのを本番へ修正**した（新規追加ではない） |
| 3. Supabaseに3つの値を設定 | ✅ 完了（`STRIPE_SECRET_KEY` は元々テスト用の鍵が入っていた＝切替は必須だった） |
| 4. コードを本番へ反映 | ✅ 完了 main `76a1f28` ／ Edge Function 3本 deploy 済み |
| 5. 通しで確認 | ⏳ **未実施** |
| 追加: `sk_live_` の再ローテーション | ⏳ **未実施**（チャットに貼られたため要再発行） |

### 4で追加で塞いだ穴（デプロイ直前に発覚）
`create-trial-subscription` が作る契約が**カード引き落とし方式のまま**だった。
カード情報を取らない運用なので、トライアル終了時に「支払い方法が無い」と判定されて
**一時停止され、請求書が発行されない**。店舗は振込先を知らないまま止まり、こちらも回収できない。
→ `send_invoice` ＋ `customer_balance(jp_bank_transfer)` ＋ `create_invoice` へ変更。
既存のカード方式の契約も `stripe-checkout` 側で振込方式へ更新するようにした。

---

## 0. 前提（確認済み）

| 項目 | 状態 |
|---|---|
| 銀行振込の審査 | ✅ **有効**（2026-08-06にStripe管理画面で確認） |
| 現在のモード | ❌ **テスト（サンドボックス）**。実店舗が契約しても料金を請求できない |
| 価格の食い違い | ❌ Stripe側が20,000円。LP・特商法は16,280円（税込） |
| 銀行振込のコード | ✅ `staging` に用意済み（`72c5260`）。本番未反映 |

**方針（2026-08-06決定）**：先着10店舗のキャンペーン中は **8,140円（税込）** を設定する。
10店舗埋まったら 16,280円（税込）に切り替える。アプリは価格を1種類しか持てない（`STRIPE_PRICE_ID`）ため、
価格IDの差し替えで運用する。**この方式だとLPの記載と実際の請求が常に一致する。**

---

## 【記録】本番モードで確定した値（2026-08-06）

| 項目 | 値 |
|---|---|
| モード | **本番**（`pk_live_51TYKXQ…` / `sk_live_…gl0L` で確認） |
| 商品ID | `prod_UkF3fyqEkqlnOU`（スタンダードプラン） |
| **8,140円（先着10店舗・いま使う）** | **`price_1U1LiiEsR4aDysj985YR1IOf`** |
| 16,280円（10店舗埋まったら差し替え） | 06/22作成済み。IDは未取得 |
| 14,800円（デフォルト・**使わない**） | 06/21作成。税抜なので使うと消費税を取りこぼす |
| 銀行振込 | **有効**（要対応0） |

---

## 1. 【今野さん】本番モードで価格を作る ✅完了

⚠️ **右上のトグルが「本番」になっていることを必ず確認**。左上のアカウント名に「サンドボックス」が
付いていたらテスト側です。

https://dashboard.stripe.com/products

1. 既存の「ShiftLink スタンダード」があればそれを開く。無ければ **商品を追加**
   - 商品名：`ShiftLink スタンダード`
2. **価格を追加**
   - 金額：**`8140`**（円。小数点なし。**税込の総額**を入れる）
   - 通貨：**JPY**
   - **継続**（Recurring）／請求間隔：**月次**
3. 作成後、価格の詳細に出る **`price_` で始まるID** をメモ

> 16,280円の価格も**今のうちに一緒に作っておく**と、10店舗埋まったときに
> IDを差し替えるだけで済みます。

---

## 2. 【今野さん】Webhookの本番エンドポイントを登録

https://dashboard.stripe.com/webhooks → **エンドポイントを追加**

- URL：
  ```
  https://qgcgkrcrfzonmmygcdju.supabase.co/functions/v1/stripe-webhook
  ```
- 送信するイベント（**この6つ**。アプリが処理しているのはこれだけ）：
  - `checkout.session.completed`
  - `customer.subscription.updated`
  - `customer.subscription.deleted`
  - `customer.subscription.trial_will_end`
  - `invoice.paid`
  - `invoice.payment_failed`

作成後、**署名シークレット（`whsec_` で始まる）** を表示してメモ。

> Webhookが無いと、入金を確認しても**アプリ側が「支払い済み」にならない**。
> 店舗は振り込んだのに使えないままになるので、ここは必須です。

---

## 3. 【今野さん】Supabaseに3つの値を設定

https://supabase.com/dashboard/project/qgcgkrcrfzonmmygcdju/settings/functions

**Edge Function Secrets** で以下を**上書き**（新規追加ではなく既存の値を書き換え）：

| キー名 | 入れる値 |
|---|---|
| `STRIPE_SECRET_KEY` | 本番の **`sk_live_`** で始まるキー（https://dashboard.stripe.com/apikeys） |
| `STRIPE_PRICE_ID` | 手順1でメモした **`price_`** |
| `STRIPE_WEBHOOK_SECRET` | 手順2でメモした **`whsec_`** |

⚠️ **`sk_live_` のキーはチャットや他のアプリに貼らないでください。** 本物のお金を動かせます。
この画面で直接入力すれば、どこにも残りません。

---

## 4. 【Claude】コードを本番へ反映

3が終わったら声をかけてください。以下を実行します。

```
# Edge Function（銀行振込版）を本番へ
supabase functions deploy stripe-checkout --project-ref qgcgkrcrfzonmmygcdju

# アプリ側（申し込みボタンの文言・振込の案内）＋特商法ページを main へ
git cherry-pick <銀行振込のコミット>
```

- `stripe-checkout` … `collection_method='send_invoice'` ＋ `jp_bank_transfer` で請求書を発行
- `index.html` … ボタンが「銀行振込で申し込む」に変わり、振込の案内文が出る
- `tokutei.html` … 支払い方法をカード→銀行振込に差し替え（staging に用意済み）

---

## 5. 【今野さん】通しで確認

**実際に申し込んで、振込先が表示されるところまで見る。**

1. https://www.shiftlink-app.jp/?signup=1 から新規登録（捨てアドレスで可）
2. 「銀行振込で申し込む」→ **お店専用の振込先が表示されるか**
3. Stripe管理画面の「請求書」に**期限7日**の請求書が立っているか

確認後、テストで作った店舗とアカウントは削除する（**店舗→アカウントの順**。逆はFK制約で失敗）。

---

## 戻し方（何かおかしくなったら）

Supabase Secrets の3つを**テスト用の値に戻す**だけで、課金は止まります。
コードは戻さなくてよい（テストキーならテストモードで動くだけ）。

`stripe-checkout` を旧版に戻す場合：
```
git show <前のコミット>:supabase/functions/stripe-checkout/index.ts > supabase/functions/stripe-checkout/index.ts
supabase functions deploy stripe-checkout --project-ref qgcgkrcrfzonmmygcdju
git checkout supabase/functions/stripe-checkout/index.ts
```

---

## 注意点

- **`APP_URL`** が未設定なら既定の `https://shiftlink-app.jp` が使われる。`www.` へ307転送されるので
  動作はするが、設定するなら `https://www.shiftlink-app.jp` が正確。
- 入金の確定に **0〜3営業日**かかる（Stripeの表示）。期限ぎりぎりの振込は着金が遅れうる。
- 振込手数料は店舗負担。特商法ページに記載済み。
- 10店舗埋まったら `STRIPE_PRICE_ID` を16,280円のIDに差し替える。
  **同時にLP・特商法ページのキャンペーン記載も消すこと**（書いてあるのに適用しないのは景表法上まずい）。
