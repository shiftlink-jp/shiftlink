# 🚨 ロールバック手順（2026-08-02 深夜作業）

## 戻し先
- **main のロールバック地点: `1f8fdf0`**（ステップ0まで適用済みの状態）
- Edge Function の戻し元コミット:
  - `pin-login` / `list-store-casts` → `1f8fdf0`（ステップ0のCORS追加込み。※`6bd9968` ではない）
  - `set-pin` → `82d1bf6`

## 手順1（最速）：Vercelで前のデプロイに戻す
Vercel Dashboard → Deployments → 1f8fdf0 のデプロイ → **Promote to Production**

## 手順2：gitで戻す
```
git push origin 1f8fdf0:main -f
```
※ SW_VER を v35 → v36 に上げ直さないと各端末のキャッシュが残る（index.html と sw.js の両方）

## 手順3：Edge Function を戻す
```
cd <1f8fdf0 をチェックアウトした worktree>
supabase functions deploy pin-login --project-ref qgcgkrcrfzonmmygcdju
supabase functions deploy list-store-casts --project-ref qgcgkrcrfzonmmygcdju
supabase functions deploy set-pin --project-ref qgcgkrcrfzonmmygcdju
```
※ set-pin のみ `82d1bf6` 版が正しい

## 緊急退避
`https://kyoukano.vercel.app` が使えない場合でも、**スタッフの端末に入っている旧PWAはキャッシュで動く可能性がある**。
本当に詰んだら Vercel の Promote が最速（1〜2分）。

## 禁止事項
- **誤ったPINでの疎通テスト禁止**（未ペアリング時は店舗共有ロック：20回/15分＋店舗バックストップ10回/60分）
